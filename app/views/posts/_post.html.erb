<p id="notice"><%= notice %></p>
<div class="row">
  <% @posts.each do |post| %>
    <div class="col s12 m6 l4">
      <div class="card large">
        <div class="card-image waves-effect waves-block waves-light">
          <%= image_tag(post.image.url(:blur), :alt => "", class: 'activator') %>
          <span class="card-title activator truncate col s12", style="background-color:rgba(0, 0, 0, 0.2);"><%= post.title %></span>
        </div>
        <div class="card-content waves-effect waves-block waves-light">
          <p class="activator"> <i><%= post.date %></i> <br/> <%= post.description %></p>
        </div>

        <div class="card-action center">
          <%
          link = '#'
          if user_signed_in?
            link = posts_check_path_url(id: post.id)
          else
            link = '#modal_thinkItsMe'
          end
          %>
          <a href="<%= link %>" id="download-button" class="btn waves-effect waves-light teal modal-trigger">Je crois que c'est moi</a>
        </div>

        <div class="card-reveal">
          <span class="card-title teal-text"><%= post.title %><i class="material-icons grey-text text-darken-4 right">close</i></span>
          <p><%= post.description %></p>
        </div>
      </div>
    </div>

  <!-- Modal view -->
  <div id="modal_thinkItsMe" class="modal modal-fixed-footer">
  	<div class="modal-content">
  		<h4>C'est moi!</h4>
  		<p>
        Si tu penses correspondre à l'annonce "<%= post.title %>", entre ton nom et prénom et sois surpris(e).
      </p>
       <h3><%= post.title %></h3>
       <%= image_tag(post.image.url(:blur), :alt => "", class: 'activator') %>
       <p>
      	<%= post.description %>
  		</p>



      <ul>
        <li><label for="user_name">Ton Nom</label>
        <input name="user[name]" id="user_name" autofocus="autofocus" required="required" type="text"></li>
        <li><label for="user_surname">Ton Prénom</label>
        <input surname="user[surname]" id="user_surname" autofocus="autofocus" required="required" type="text"></li>
      </ul>

  	</div>
  	<div class="modal-footer">
      <a href="<%= posts_check_path_url(id: post.id) %>" class="modal-action modal-close waves-effect waves-green btn-flat">Verifier</a>
  		<a href="<%= new_user_session_path %>" class="modal-action modal-close waves-effect waves-green btn-flat">Login</a>
      <a href="<%= new_user_registration_path %>" class="modal-action modal-close waves-effect waves-green btn-flat">S'enregistrer</a>
  	</div>
    <% end %>
</div>
