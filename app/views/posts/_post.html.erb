<p id="notice"><%= notice %></p>
<div class="row">
  <% @posts.each do |post| %>
    <div class="col s12 m6 l4">
      <div class="card large">
        <div class="card-image waves-effect waves-block waves-light">
          <%= image_tag(post.image.url(:blur), :alt => "", class: 'activator') %>

        </div>

        <div class="card-content">
          <span class="card-title activator truncate grey-text text-darken-4"><%= post.title %></span>
          <p class="activator"> <i><%= post.date %></i></span>
        </div>

        <div class="card-action center">
          <a href="<%= posts_check_url(id: post.id) %>" id="download-button" class="btn waves-effect waves-light teal">C'est moi!</a>
        </div>

        <div class="card-reveal">
          <span class="card-title teal-text"><%= post.title %><i class="material-icons grey-text text-darken-4 right">close</i></span>
          <p><%= post.description %></p>
        </div>
      </div>
    </div>
  <% end %>
</div>
