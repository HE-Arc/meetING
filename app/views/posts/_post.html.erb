<p id="notice"><%= notice %></p>
<div class="row">
  <% @posts.each do |post| %>
    <div class="col s12 m6 l4">
      <div class="card large">
        <div class="card-image">
          <%= image_tag(post.image.url(:blur), :alt => "") %>
          <span class="card-title truncate col s12", style="background-color:rgba(0, 0, 0, 0.2);"><%= post.title %></span>
        </div>
        <div class="card-content">
          <span class="card-date activator grey-text text-darken-4"><i><%= post.date %></i><i class="material-icons right teal-text">more_vert</i></span>
          <p><%= post.description %></p>
        </div>

        <div class="card-action center">
          <a href="/posts/<%= post.id %>/check" id="download-button" class="btn waves-effect waves-light teal">Je crois que c'est moi</a>
        </div>
        
        <div class="card-reveal">
          <span class="card-title teal-text"><%= post.title %><i class="material-icons grey-text text-darken-4 right">close</i></span>
          <p><%= post.description %></p>
        </div>
      </div>
    </div>
  <% end %>
</div>
